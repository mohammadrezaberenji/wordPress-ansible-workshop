---
- name: Install nginx and add user 
  hosts : all
  tasks:
    - name : copy script 
      copy:
        src: install_nginx.sh
        dest: ~/new-test
        
    - name: Run script 
      shell: \~/new-test/install.nginx.sh 
